# Expense Tracker

## Follow the step for running project

#### Step-1: Download project
#### Step-2: Run the command composer install
#### Step-3: Create a database. the name will be "monthly_expense_report"
#### Step-4: Run the command php artisan migrate:fresh --seed
#### Step-5: Run the command npm install && npm run dev
#### Step-6: Run the command php artisan ser
